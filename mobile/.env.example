# Backend URL. Leave unset during development on the same machine —
# the app derives it from the Metro bundler host automatically.
# Set it explicitly when pointing at a deployed server:
# EXPO_PUBLIC_API_URL=https://notes.example.com/api
EXPO_PUBLIC_API_URL=
